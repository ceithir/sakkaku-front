import React from "react";
import Title from "features/display/Title";

const CustomTitle = () => {
  return <Title>{`Legend of the Five Rings (AEG) – D10 Roll & Keep`}</Title>;
};

export default CustomTitle;
